# This config was automatically generated from your source code
# Stacks detected: deps:java:source/clicklogger
version: 2.1
jobs:
  java-build:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/source/clicklogger
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          command: mvn verify
      - store_test_results:
          path: target/surefire-reports
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.m2/repository
  threat-model:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Terraform Init
          command: '# terraform init'
      - run:
          name: Terraform Plan Generation
          command: '# terraform plan -out tfplan'
      - run:
          name: Terraform Plan Show
          command: '# terraform show -json tfplan > tfplan.json'
      - run:
          name: Terraform Graph file
          command: '# terraform show -json tfplan > tfplan.json'
      - run:
          name: Update Threat Model
          command: '# '
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: Build or Update Infrastructure
          command: '# terraform apply tfplan --auto-approve'
workflows:
  cicd:
    jobs:
      - java-build
#      - threat-model:
#        requires:
#          - java-build
#      - deploy:
#         requires:
#          - threat-model
#
